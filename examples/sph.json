{
    "dimension": 3,
    "domains": {
        "t1": 0,
        "t2": 21,
        "x1": 0,
        "x2": 21,
        "y1": 0,
        "y2": 21
    },
    "functions": {
        "color-function": "main: (i) => {\n\tfunction color(i) {\n\t\tlet c = i\n\t\tlet h = i * 6\n\t\tlet l = 100-i * 4\n\t\tlet s = 40\n\t\tlet rgb = hslToRGB([h,s,l])\n\t\treturn colorHelper(rgb[0], rgb[1], rgb[2])\n\t}\n\n\n/*\n * https://www.30secondsofcode.org/js/s/hsl-to-rgb/\n */\nfunction hslToRGB(hsl) {\n  const h = hsl[0];\n  const s = hsl[1] / 100;\n  const l = hsl[2] / 100;\n  const k = (n) => (n + h / 30) % 12;\n  const a = s * Math.min(l, 1 - l);\n  const f = (n) =>\n    l - a * Math.max(-1, Math.min(k(n) - 3, Math.min(9 - k(n), 1)));\n  return [Math.floor(255 * f(0)), Math.floor(255 * f(8)), Math.floor(255 * f(4))];\n}\n\n\n\tfunction colorHelper(r, g, b) {\n\t\treturn b + g * 0x100 + r* 0x10000\n\t}\n\treturn color(i)\n}",
        "pixel-function": "main: (x, y, z) => { \n\tfunction calc(x, y, z) { \n\t\treturn ((x-10)**2 + (y-10)**2 + (z-10)**2) % 20\n\t} \n return calc(x, y, z) \n}"
    }
}
